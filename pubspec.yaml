name: workshop_wizard
description: "Workshop Wizard Sprint 1 Final"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  
  # Emergency Stable Versions for 12 PM Presentation
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12
  firebase_crashlytics: ^4.3.0
  
  # Teammate's UI Dependencies
  printing: ^5.14.3
  intl: ^0.20.2
  pdf: ^3.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
